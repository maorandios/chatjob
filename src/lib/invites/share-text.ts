export function getInviteShareText(name: string, inviteUrl: string): string {
  return `היי ${name}, קיבלת הזמנה להצטרף לקלינג - תקשורת בין עובדים זרים, בעברית.
בקישור המצורף תוכל ליצור חשבון חדש ולדבר בשפה שלך עם כולם.

${inviteUrl}`;
}
